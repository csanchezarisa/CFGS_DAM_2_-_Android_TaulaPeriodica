<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.taulaperidica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaulaPeriòdica">
        <activity android:name=".game.modalitats.ActivityGameEncertarNumero"
            android:screenOrientation="portrait">

        </activity>
        <activity android:name=".game.modalitats.ActivityGameEncertarEstat"
            android:screenOrientation="portrait" />
        <activity android:name=".game.modalitats.ActivityGameEncertarSimbol"
            android:screenOrientation="portrait" />
        <activity android:name=".game.modalitats.ActivityGameEncertarNom"
            android:screenOrientation="portrait" />
        <activity
            android:name=".game.ActivityGame"
            android:configChanges="orientation|screenSize" />
        <activity
            android:name=".ActivityDadesElement"
            android:screenOrientation="portrait">
            <intent-filter
                android:scheme="http"
                tools:ignore="AppLinkUrlError">
                <action
                    android:name="android.intent.action.VIEW"
                    tools:ignore="AppLinkUrlError" />
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>